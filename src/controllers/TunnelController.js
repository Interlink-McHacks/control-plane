const Tunnel = require('../models/Tunnel');
const TunnelController = {};

TunnelController.createTunnel = function (name, description, hostID, tenantID, hostConnectPort, wgListeningPort, type) {

}

module.exports = TunnelController;